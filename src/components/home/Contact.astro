---
import Logo from "../../assets/logo.svg";
---

<section class="flex justify-center w-full">
  <div
    class="rounded-3xl flex flex-col gap-12 bg-white p-8 shadow-sm md:flex-row md:items-center md:p-12"
  >
    <div class="flex flex-col flex-1 gap-5 h-full justify-start">
      <div class="flex flex-col justify-center items-center gap-5">
        <img src={Logo.src} alt="" class="w-17" />
        <h2 class="text-2xl font-bold text-gray-900 md:text-3xl text-center">
          ¿Tenés alguna sugerencia?
        </h2>
      </div>

      <p class="text-gray-600">
        Este portal se construye entre todos. Queremos que sea útil, claro y
        actualizado.
      </p>

      <ul class="space-y-1 text-gray-700">
        <li class="flex gap-3">
          <span class="text-primary">✔</span>
          Si encontrás un error o falta información
        </li>
        <li class="flex gap-3">
          <span class="text-primary">✔</span>
          Si querés publicar tu negocio o servicio
        </li>
        <li class="flex gap-3">
          <span class="text-primary">✔</span>
          Si tenés una idea para mejorar la página
        </li>
      </ul>

      <p class="text-gray-600">
        Escribinos y ayudanos a seguir mejorando este espacio para toda la
        comunidad.
      </p>
    </div>

    <form
      class="space-y-6 flex flex-col flex-1"
      action="mailto:contacto@eltalar.com.ar"
      method="POST"
      enctype="text/plain"
    >
      <div>
        <label class="block text-sm font-medium text-gray-700">Nombre</label>
        <input
          type="text"
          name="nombre"
          placeholder="Tu nombre"
          required
          class="mt-2 w-full rounded-xl border border-gray-200 px-4 py-3 focus:border-primary focus:outline-none focus:ring-2 focus:ring-primary/20"
        />
      </div>

      <div>
        <label class="block text-sm font-medium text-gray-700">Email</label>
        <input
          type="email"
          name="email"
          placeholder="tu@email.com"
          required
          class="mt-2 w-full rounded-xl border border-gray-200 px-4 py-3 focus:border-primary focus:outline-none focus:ring-2 focus:ring-primary/20"
        />
      </div>

      <div>
        <label class="block text-sm font-medium text-gray-700">Mensaje</label>
        <textarea
          name="mensaje"
          rows="4"
          placeholder="Contanos tu sugerencia, comentario o consulta..."
          required
          class="mt-2 w-full resize-none rounded-xl border border-gray-200 px-4 py-3 focus:border-primary focus:outline-none focus:ring-2 focus:ring-primary/20"
        ></textarea>
      </div>

      <button
        type="submit"
        class="w-full rounded-xl bg-primary px-6 py-3 font-semibold text-white transition-transform hover:bg-green-700 hover:scale-102"
      >
        Enviar mensaje
      </button>

      <p class="text-center text-xs text-gray-500">
        O escribinos a
        <a
          href="mailto:contacto@eltalar.com.ar"
          class="font-medium text-primary hover:underline"
        >
          contacto@eltalar.com.ar
        </a>
      </p>
    </form>
  </div>
</section>
